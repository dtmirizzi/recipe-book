# deps
node_modules
.pnpm-store

# env
.env
.env.local
.env.*.local

# build
.next
out
dist
build
next-env.d.ts

# turbo / vercel
.turbo
.vercel

# testing
coverage
playwright-report
test-results
.nyc_output

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# editors
.vscode/*
!.vscode/extensions.json
.idea

# local uploads (dev blob fallback)
public/uploads/

# Pexels video candidates — raw clips are bulk binary (re-fetchable via
# the manifest if needed). The curated output ships in /public/videos/.
design/video-candidates/*.mp4
design/video-candidates/thumbs/
design/video-candidates-2/*.mp4
design/video-candidates-2/thumbs-raw/
design/video-candidates-2/thumbs-styled/

# db data
postgres-data/
tsconfig.tsbuildinfo
